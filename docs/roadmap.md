# Roadmap (Somewhat in order)

- Implement the remaining target-language features from `README.md`
- Tighten parse and runtime error messages
- Add semantic analysis for invalid control flow and other static checks
- Add `print` and `type()` built-ins
- Implement strings, floats, arrays, tables, closures, and first-class functions
- Implement `return`, multiple functions, parameters, and call expressions
- Better error messages / recovery
- Bytecode VM
- Heap types
- Garbage collector
- JIT
